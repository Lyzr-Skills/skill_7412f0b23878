# Installation

To install this skill under your target GTMOS agent workspace:
1. Clone this directory structure under `projects/skills/`.
2. Configure settings inside `config/defaults.yaml`.
3. Reference this skill name in your `agent_config.json` extensions mapping.
