# Configuration Guide

Configure execution metrics inside the config package:
- `defaults.yaml`: strict mode flag, drift detection schedules.
- `scoring.yaml`: weights assigned to problem severity, frequency, product fit.
- `schema.json`: structural JSON definitions validating input limits.
