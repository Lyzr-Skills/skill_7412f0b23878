# Usage Guide

Execute the pipeline using the command line framework or from parent agent instructions:

## Execution Steps
1. Feed raw evidence logs to `evidence/` directory.
2. Initialize stage processing scripts in sequence.
3. Review final verification logs inside the generated report.
