# Customer Evidence Collection Module

Aggregates structured and unstructured data feeds. Converts call transcripts, forum discussions (Reddit, communities), survey logs, and support tickets into standardized evidence logs mapping back to specific source references (`EV-xxx`).

## Schema Validation
Each generated object MUST validate against `schemas/evidence.json`.
